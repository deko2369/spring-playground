logging:
  level:
    root: INFO

vcap:
  services:
    mysqlservice:
      credentials:
        username: testuser
        password: testpassword
        host: testhost
        port: 10
    ikadb:
      credentials:
        jdbc-url: jdbc:sqlite:ika.sqlite